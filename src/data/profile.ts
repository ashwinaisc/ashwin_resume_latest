import raw from './profile.json';
import type { Profile } from './types';

// Transcribed from public/ashwin-resume.pdf. Selected work summarizes employment
// responsibilities; it does not claim named client case studies or measured results.
export const profile = raw as Profile;
